spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    hibernate:
      ddl-auto: create  # (1) ??? ?? ??
    #    show-sql: true      # (2) SQL ?? ??
    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.SQL: debug